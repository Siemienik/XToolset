{
    "name": "xlsx-template-engine",
    "version": "1.0.0",
    "description": "",
    "main": "index.js",
    "scripts": {
        "example": "cd build && node example.js",
        "build": "node_modules/.bin/babel -d build -s true src ",
        "watch": "node_modules/.bin/babel -d build -s true src --watch",
        "test": "echo \"Error: no test specified\" && exit 1"
    },
    "author": "Paweł Siemienik",
    "devDependencies": {
        "babel-cli": "^6.26.0",
        "babel-preset-es2015": "^6.24.1",
        "babel-preset-es2017-node7": "^0.5.2"
    }
}
